# 📅 日程管理系统

## 🌐 网页地址
https://annoyinghh.github.io/schedule/

## ⚙️ 系统架构

### 网页端（浏览器）
- 管理日程、打卡、添加事件
- 数据保存在浏览器 localStorage
- 每天打开网页时自动检查提醒

### 云服务器端（自动提醒）
- 每天早上 7:00 自动检查并发送微信提醒
- 使用内置的默认事件数据

## 📝 如何添加新事件

### 1. 在网页上添加
打开网页 → 点击月历 → 添加事件

### 2. 同步到云服务器（手动）
因为HTTPS限制，需要手动同步：

```bash
# 连接云服务器
ssh root@171.80.9.175

# 编辑提醒脚本
nano ~/schedule-reminder/cloud_reminder.js

# 找到 milestones 部分，添加新事件：
milestones: [
    { date: "2026-03-15", name: "春季大考 (预估)", note: "3月1日开始停止娱乐，全力冲刺" },
    { date: "2026-01-27", name: "你的新事件", note: "备注" }  // 添加这一行
],

# 找到 calendarEvents 部分，添加月历事件：
calendarEvents: {
    "2026-01-20": "xzx",
    "2026-01-25": "新事件"  // 添加这一行
}

# 保存：Ctrl+X, Y, Enter

# 测试
cd ~/schedule-reminder
node cloud_reminder.js
```

## 🔧 云服务器管理

### 查看API服务器状态
```bash
ps aux | grep api_server
```

### 重启API服务器
```bash
pkill -f api_server
cd ~/schedule-reminder
nohup node api_server.js > api.log 2>&1 &
```

### 查看提醒日志
```bash
tail -f ~/schedule-reminder/reminder.log
```

### 手动测试提醒
```bash
cd ~/schedule-reminder
node test_reminder.js  # 测试通知
node cloud_reminder.js  # 测试真实提醒
```

### 查看定时任务
```bash
crontab -l
```

## 📋 文件说明

- `index.html` - 网页（唯一需要的网页文件）
- `api_server.js` - API服务器（云服务器）
- `cloud_reminder.js` - 提醒脚本（云服务器）
- `test_reminder.js` - 测试脚本（云服务器）
- `deploy.sh` - 部署脚本（云服务器）

## 🆘 常见问题

### Q: 网页添加的事件没有提醒？
A: 需要手动同步到云服务器（见上方"如何添加新事件"）

### Q: 没有收到微信通知？
A: 
1. 检查是否关注了「方糖服务号」
2. 运行测试脚本：`node test_reminder.js`
3. 查看日志：`tail -f ~/schedule-reminder/reminder.log`

### Q: API服务器无法访问？
A: 因为HTTPS限制，API同步已禁用。使用手动同步方式。

## 📞 联系方式
- 手机: 15914969166
- 邮箱: 1875512848@qq.com
- Server酱: 方糖服务号
